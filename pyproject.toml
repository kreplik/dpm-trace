[project]
name = "dpm-trace"
version = "0.1.0"
description = "CLI-only POC for participant-scoped Canton transaction tracing and interactive stepping."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
dpm-trace = "dpm_trace.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
